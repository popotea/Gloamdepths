# 《微光深淵》未來開發規劃

這份文件整理目前已有的系統範圍、以及規劃中的新功能——每項都附上「為什麼適合」「大概工程量」「跟其他系統的相依關係」「開工前要先想清楚的設計問題」。用來決定優先順序,不是寫死的排程,隨時可以調整。

---

## 目前已有系統(基準線,避免規劃重複)

- **核心循環**:星核能量流失/餵光晶、暗潮圍攻、三神殿守衛、3 塊碎片、最終暗潮通關
- **採集**:木/銅/鐵/金/鑽石鎬分級、砂礫/煤/鑽石礦、光晶、木根
- **戰鬥**:劍(自動)、矛(手動/長距離)、鎚(手動/範圍+擊退)、弓/弩(彈藥)、光晶法杖(貫穿);焰/冰/光/重擊 4 種元素相剋
- **敵人**:9 種蝕影(含成群/遠程/自爆/穿牆/拆牆特性)+ 2 種神殿 Boss(石像守衛 earth / 熔岩魔像 fire,2026-07 起三神殿各自固定分配,原 3.2)
- **防禦建築**:火把、木/石牆、光塔(自動)、箭塔(彈藥制+每人數量上限);**已放置的建築(工作台/熔爐/光塔/箭塔/火把)左鍵敲幾下可拆回背包道具,能帶去別處重新放置**
- **據點**:神殿(Boss)、廢墟(寶箱)、蝕影巢穴(持續生怪,可拆除,2026-07 起分三種:一般/孢群/精英)
- **成長**:等級/經驗(+HP/+攻擊%)、**天賦樹**(每級 1 點,T 鍵分配 6 種被動;2026-07 完成,原 2.2)、強化卷軸(+1~+5 衝裝)、體力/衝刺
- **農耕**:鏟子翻土、種子、三階段成長、自動收成(2026-07 剛完成)
- **料理 buff**:光傘菇湯(移速)/岩鎧燉菜(減傷)/礦工大餐(挖掘)/晶鱗魚湯(回春),吃了掛短時 buff(2026-07 完成,原 1.1)
- **釣魚**:幽光水池地形、釣竿拋竿計時出魚、魚類食材接料理(2026-07 完成,原 2.1)
- **圍籬**:矮柵欄擋怪不擋箭,圈農地/未來牧場用(2026-07 完成,原 1.2)
- **動物養殖**:幽穴雞/苔絨牛,野外偶遇→飼料引誘跟隨→圍籬圈養→餵食產蛋/奶,近戰宰殺掉肉(2026-07 完成,原 2.4)
- **多人**:PeerJS 房主權威連線、存檔在房主、玩家背包依名字保存、聊天(含歷史紀錄可 ↑/↓ 叫回)
- **秘笈選單** `/power`:全地圖開亮、召喚怪物、星核/暗潮控制等除錯指令
- **大地圖**:M 鍵開全螢幕地圖,滾輪縮放、拖曳平移,標記圖示+滑鼠靠近顯示 tooltip(2026-07 完成,原 1.4)
- **NPC 商人**:中層區域隨機生成的固定攤位,交易面板依星核碎片數(0~3)分階解鎖(2026-07 完成,原 2.3)

---

## 一、短期(小工程,直接接在現有系統上)

### ~~1.1 料理擴充~~ ✅ 已完成(2026-07-10)
- 帶 buff 的料理 ×4(見上方基準線);buff 種類定義在 `BUFF_INFO`(config.js),
  生效點:speed=main.js localControl、mine=doMine、guard=damagePlayer、regen=updatePlayersHost

### ~~1.2 圍籬/柵欄~~ ✅ 已完成(2026-07-10)
- `T.FENCE` 地形,`low: true` 讓投射物飛得過(`projHitsWall`,world.js);耐久 25 比木牆(40)脆

### ~~1.3 統計面板~~ ✅ 已完成(2026-07-10)
- 定案:擊殺數、存活時間、暗潮波數三項(不含最高等級,玩家列表已顯示各自等級)
- 只有擊殺數是真正新增的計數器(`G.killCount`,`killEnemy` 開頭累加);存活時間/暗潮波數
  直接讀既有的 `G.time`/`G.wave.n`,不用新欄位。`buildSave`/`applySave`/`snap` 都補上
  `killCount`,客戶端也能即時看到全隊統計(不只房主)。ESC 選單新增「📊 統計」分頁,
  沿用 `UI.menuView` 既有的子頁面切換模式(`ui.js` `renderMenu`)。

### ~~1.4 大地圖(M 鍵開全螢幕地圖 + 滾輪縮放)~~ ✅ 已完成(2026-07-10)
- 按 M 開全螢幕 `#mapPanel`(`ui.js` `toggleMapPanel`),重用 `drawMinimap()` 的離線 ImageData(`UI.mmImage`)
  整倍縮放貼圖,滾輪縮放(`UI.mapZoom` 1~12)、拖曳平移(`UI.mapPanX/Y`);星核/神殿/巢穴標記換成
  圖示+滑鼠靠近顯示 tooltip(`mapMarkers()`),Esc/M 都可關閉

---

## 二、中期(新系統,獨立模組,量體跟這次農耕差不多)

### ~~2.1 釣魚~~ ✅ 已完成(2026-07-10)
- `T.WATER` 幽光水池(生成規則:外圈一圈必須全是地板才動工,保證繞得過去不堵通道)、
  釣竿(`fish: true`)、`doFish` 拋竿 + `FISH_CFG` 掉落表(config.js)、魚接進料理(烤魚/晶鱗魚湯)

### ~~2.2 天賦樹~~ ✅ 已完成(2026-07-10)
- 定案:不做「升級隨機 3 選 1」,改成每級 1 點、T 鍵面板自由分配(`TALENTS` 6 種個人被動,
  總階數 15 > 滿級 9 點,取捨壓力還在);與衝裝疊加的顧慮用「數值小一階」處理
- 之後想加全域型天賦(如星核減傷)再回來討論多人歸屬/疊加規則

### ~~2.3 NPC 商人~~ ✅ 已完成(2026-07-10)
- 定案:不放星核旁,改成跟神殿/巢穴一樣在**中層區域(zone 1)隨機生成**,需探索才找得到;
  匯率表`TRADER_CFG`(config.js)依**星核碎片數(0~3)**分階累加開放(與已擊敗神殿數一對一對應,
  且已是即時同步資料,不用額外做網路同步),右鍵開點擊式交易面板(`doTrade`,仿 /power 選單風格)
- 商人是獨立於 `G.objects` 的固定實體(`G.traders`),不會動、不能被攻擊/挖掘;
  貼圖走跟怪物同一套「Image 快取+找不到檔案自動退回 emoji 畫法」機制(`assets/npcs/trader.png`)

### ~~2.4 動物養殖~~ ✅ 已完成(2026-07-10)
- `ANIMAL_TYPES`(config.js)幽穴雞(蛋)/苔絨牛(奶),`updateAnimals` 獨立被動 AI;
  設計問題的定案:動物來源=**野外偶遇**(泥土區遊蕩+緩慢自然補充,手持飼料引誘跟隨),
  光照=不額外處理(怪本來就不打動物,黑暗生怪機制不用改)
- 之後想加新動物(羊/豬)只要在 `ANIMAL_TYPES` 加一筆(可能要配新產物/配方)

### ~~2.5 工具/武器耐久度~~ ✅ 已完成(2026-07-10,溫和版)
- 定案:鎬/武器有耐久(鏟子/釣竿/護甲豁免),歸零「損壞停用」**永不消失**(衝裝投資不蒸發),
  工作台修理=合成成本一半,衝裝每級 +15% 耐久上限;成功命中/挖掘才磨損
- 以下保留原始評估備查:

### (原)2.5 工具/武器耐久度
- **現況**:所有鎬/劍/矛/鎚/弓弩/法杖都是永久耐用,不會壞
- **做法草案**:給 `ITEMS` 裡的 `pick`/`sword`/`ranged` 物品加一個 `durability` 欄位(最大耐久),背包格子上的裝備物件(`{id, count, lv, ...}`)多存一個 `dur` 目前耐久;每次 `doMine`(挖礦)、`doSwing`(揮擊)、`doShoot`(射擊)成功命中就扣 1 點,扣到 0 就損毀(從背包移除,可以做成直接消失或掉一個「壞掉的 XX」殘骸道具);UI 在快捷欄/背包格子的物品圖示下方加一條像血條的耐久條
- **工程量**:中,牽動的呼叫點不少(挖礦/近戰/遠程都要扣耐久並檢查損毀),但每一處改動都不大,風險可控
- **相依**:無,但要跟以下系統一起規劃數值,不然體驗會很痛:
  - **強化卷軸(ENH_CFG)**:裝備砸了好幾張卷軸衝到 +5,結果耐久用完直接消失,會讓玩家很挫折——建議衝裝時一併增加耐久上限,或乾脆衝到某個等級後耐久免疫
  - **修理機制**:要不要能在工作台花材料補耐久?(不做的話,耐久系統本質上是「裝備都是消耗品」,對玩家心理負擔較大,需要謹慎評估要不要做這麼硬核)
- **⚠️ 這是一個會改變遊戲手感的系統,建議先想清楚一個問題再開工**:目前微光深淵的裝備成長線是「打怪→掉卷軸→衝裝越打越強」的正向循環,耐久度是「用越多壞越快」的負向摩擦,兩者疊加要抓好平衡,否則後期玩家會花大量時間打材料修裝備而不是探索/防守,偏離核心玩法

---

## 三、長期(較大規模,會動到核心迴圈,建議前面幾項穩定後再排)

### 3.1 通關後內容
- 現在打完最終暗潮就結束(`gameOver(true)`),之後只能自由遊玩
- 選項 A:無盡模式(暗潮持續增強,類 roguelite 刷分/刷紀錄)
- 選項 B:解鎖第五區域(比黑曜區更深,更強的礦物/怪物)
- **工程量**:大,不管哪個選項都牽動 `WAVE_CFG`/`ENEMY_TYPES` 的無上限強度曲線設計,需要仔細調校

### 3.2 更多 Boss 種類(拆三次上線)
- 第 1 隻(火系)✅ 已完成(2026-07-10):`fire_boss`「熔岩魔像」,遠程吐火球+落地/命中範圍爆炸
  (`spawnProj`/`updateProjs` 新增 `aoe` 欄位機制,不是新的 `ENEMY_TYPES` 行為擴充),
  `elem:'fire'` 讓冰系武器自動剋制、火系武器自動打折(既有 `ELEM_VS` 機制免改)。
  神殿死亡判斷重構成通用(`e.home` 而非寫死 type),搭配 `SHRINE_BOSS_LOOT` 資料表,
  之後補第 2、3 隻只要在 `world.js` 的 `SHRINE_BOSSES` 陣列換 key、config.js 加一筆敵人資料,
  不用再碰 `killEnemy`/存讀檔/net 同步(已在架構上打通)。
- 第 2 隻(冰系,下一個)、第 3 隻(穿牆系):待做,呼應現有元素相剋系統
- 目前仍缺美術素材(`assets/monsters/fire_boss.png` 不存在),自動 fallback 成向量畫法(color/eye 換色區隔),不影響遊戲運作

### 3.3 難度選項
- 開局選輕鬆/一般/困難,調整星核流失速度、暗潮強度倍率
- **工程量**:小中,主要是把 `CORE_CFG`/`WAVE_CFG` 的固定數值改成依難度縮放,UI 加一個開局選項
- **相依**:建議放在 3.1/3.2 都上線、整體數值曲線比較穩定後再做,不然每次調怪物數值都要重新過一次三種難度

### 3.4 多存檔/世界選擇
- 現況:`localStorage` 只認一份存檔(`gloamdepths_save` 這個固定 key),開新世界就會覆蓋掉舊的
- 做法:存檔 key 改成陣列/清單(如 `gloamdepths_save_1`、`_2`),主選單加「選擇存檔」畫面
- **工程量**:中,主要是選單 UI + 既有存讀檔函式的 key 從寫死改成參數化,风险較低但要仔细測試不要弄丟現有玩家的舊存檔(需要寫一個「偵測到舊格式存檔,自動搬進新格式」的相容邏輯)

### 3.5 鐵軌與礦車(快速移動)
- **做法**:新增一種可放置的軌道地形(跟木牆/石牆一樣的「放置地形」機制),站上去移動速度大幅提升(如 ×2.5~3),用來打通星核基地跟遠方礦區/神殿之間的快速通道
- **v1 建議做法(較簡單)**:軌道只是「加速地板」,不做真的會轉彎、有物理的礦車實體——移動邏輯照抄現有 `localControl` 的移速判定,偵測腳下 tile 是軌道就加乘速度即可
- **v2(進階,先不做)**:真正的礦車實體會沿軌道自動前進、遇轉轍器分岔——需要一套軌道連通圖 + 尋路邏輯,複雜度接近 Terraria 礦車/Factorio 火車軌道系統,不建議一開始就做
- **工程量**:中(v1)
- **相依**:無

### 3.6 傳輸帶 + 自動採礦
- **做法**:自動採礦機(放置建築,行為模式參考現有光塔/箭塔的「自動觸發」設計)靠近礦脈會定時自動採集(仍受鎬階級限制,金鎬機才挖得動鑽石礦);傳輸帶(跟軌道一樣是放置地形,但功能是把地上的掉落物往指定方向推)把礦物送回基地儲物箱
- **⚠️ 設計張力,開工前必須先決定**:全自動化會削弱「黑暗會生怪、玩家得主動冒險挖礦」這個核心迴圈的存在意義。建議的折衷方向(擇一或並用):
  1. 自動採礦機**只能架在玩家已經探索過、光照覆蓋的區域**(玩家還是得先冒險把地圖點亮才能佈署)
  2. 產量**遠低於手動**採礦(定位成「後期省事的 QoL」,不是「取代探索的效率解」)
  3. 自動採礦機本身也需要光晶持續供電才能運作,間接跟星核資源迴圈掛勾
- **工程量**:大(新地形類型 + 新建築 AI + 物品在傳輸帶上移動的渲染與邏輯,是這批裡最重的一項)
- **相依**:建議在 3.5 軌道做完、拿到「放置地形+高速移動」的經驗後再做,傳輸帶的物品搬運邏輯某種程度上是軌道機制的延伸

---

## 四、基礎建設(不是新玩法,是讓現有連線更穩)

### 4.1 房主斷線轉移
- 現況:房主關頁面,遊戲直接結束,其他人斷線
- 做法:偵測房主斷線後,其中一位客戶端升級成新房主(需要把當下的世界狀態從某個客戶端重建,或定期讓客戶端也持有一份唯讀快照)
- **工程量**:大,牽動整個連線架構的「房主權威」假設,是目前系統風險最高的一項改動

### 4.2 TURN 中繼伺服器
- 解決極少數嚴格 NAT 網路環境連不上的問題(玩法設計文件本來就列為已知限制)
- **工程量**:中,主要是設定成本(要找免費/低成本 TURN 服務或自架),程式面只要 PeerJS 設定加一組 ICE server 設定值,改動很小

### 4.3 觀戰模式
- 死亡或想旁觀時可以自由移動鏡頭看隊友
- **工程量**:小中,`render.js` 的攝影機邏輯要能脫離 `myPlayer()` 位置,新增一個觀戰狀態機

---

## 建議排序(僅供參考,四個大方向都值得做)

```
第一批 ✅ 全部完成(2026-07-10):
  1.1 料理擴充 → 2.1 釣魚 → 1.2 圍籬

第二批 ✅ 全部完成(2026-07-10):
  2.4 動物養殖 → 2.2 天賦樹

第三批(視第一二批的手感決定要不要繼續加深度):
  2.5 耐久度 ✅ 完成(2026-07-10)→ 2.3 NPC 商人 ✅ 完成(2026-07-10)
  → 1.3 統計面板 ✅ 完成(2026-07-10)→ 3.2 更多 Boss:第1隻火系 ✅ 完成(2026-07-10),
  第2隻冰系(下一個)→ 3.3 難度選項

第四批(長期投資,建議最後排):
  3.5 鐵軌快速移動 → 3.6 傳輸帶+自動採礦(3.6 開工前務必先決定設計張力的折衷方案)
  → 3.1 通關後內容 → 3.4 多存檔 → 4.1 房主轉移
```

4.2(TURN 中繼)工程量很小、不影響其他系統,不受這個排序限制,想插隊隨時可以做。
